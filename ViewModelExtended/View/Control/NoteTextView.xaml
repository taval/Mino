<UserControl
	x:Class="ViewModelExtended.NoteTextView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:local="clr-namespace:ViewModelExtended"
	mc:Ignorable="d" 
	d:DesignHeight="450" d:DesignWidth="800">
	<Grid
		Height="{ Binding Parent.ActualHeight, Mode=OneWay, RelativeSource={ RelativeSource Self } }"
		Width="{ Binding Parent.ActualWidth, Mode=OneWay, RelativeSource={ RelativeSource Self } }">
		<Grid.RowDefinitions>
			<RowDefinition Height="auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<TextBox
			Margin="0 16 0 8"
			Height="32"
			Grid.Row="0"
			FontSize="24"
			Style="{ StaticResource warnHighlightStyle }"
			Text="{ Binding Title, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True }" />
		<TextBox
			Grid.Row="1"
			Text="{ Binding Path=Text, UpdateSourceTrigger=PropertyChanged }" />
	</Grid>

</UserControl>
